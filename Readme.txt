# This peoject will search a HOT-WORD you want in all the file present in All the directories RECURSIVELY
# and save all those files in a new Folder (named: HOT_FOLDER) 
#and append those line with their file number into those files at the End.